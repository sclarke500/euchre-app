/**
 * Chat System Types
 *
 * Only shared chat type definitions remain (e.g. ChatMode, used by the server
 * games). The phrase pools / picker / per-game chat engines were dead code and
 * have been removed — live bot remarks come from ../bots (getRemark).
 */

export * from './types.js'
